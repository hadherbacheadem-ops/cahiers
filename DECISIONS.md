# Décisions de conception

Journal des choix non tranchés par le cahier des charges « Évolution fondée sur la recherche en sciences cognitives », avec la raison. Chaque phase ajoute sa section.

## Phase 0 — État des lieux et socle de données

### État des lieux (avant la phase)

- **Données** : Dexie v2, tables `cahiers / chapitres / exercises / attempts / settings / supplements / mindmaps`, timestamps en epoch ms. Un exercice = 6 types (`flashcard, cloze, mcq, truefalse, match, order`), `difficulty` 1–3, `tags`, état SM-2 `{ease, interval, due, reps, lapses}`. Aucun lien exercice → passage de la fiche, aucun statut (tout est actif dès l'import), pas de file « à valider ».
- **Journal** : `attempts` existait (exercice, ts, juste/faux, note, mode, durée) mais **tous les modes modifiaient le planning**, Chrono et S'entraîner compris. Pas de confiance, pas d'annulation.
- **Planificateur** : SM-2 simplifié (`lib/srs.ts`) — un échec remet `interval` et `reps` à zéro, plancher d'ease 1,3 (« ease hell »).
- **Sessions** : Réviser = dues triées par échéance, plafond 50, sans entrelacement ni gestion des siblings ; S'entraîner = 30 au hasard, ratés re-proposés ; Chrono = feedback immédiat, pas différé. Pas d'undo, pas d'édition en session, pas de leech.
- **Génération** : prompt « exhaustif » mais sans extraction de points de cours, sans ancre, sans schéma JSON strict ; validation zod des types à l'import ; pas de linter, pas de dédoublonnage.
- **Rendu** : `.prose-fiche` = texte brut (`white-space: pre-wrap`). Ni markdown ni LaTeX. Fiches texte seul, pas d'images stockées.
- **Sauvegarde** : JSON `version 1|2`, import par `bulkPut` sans migration ; les `attempts` étaient ré-ajoutés à chaque import (doublons).
- Ni tests, ni dépôt git, pas de PWA.

### Décisions

1. **Noms internes conservés.** Le cahier des charges parle de `ficheId`, `statut`, `source`, `affecteLePlanning` ; le code existant utilise `chapitreId`, des identifiants anglais et des timestamps numériques. On garde la convention du code (anglais, epoch ms) pour ne pas créer deux vocabulaires : `ficheId` → `chapitreId`, `statut` → `status` (`pending | active | suspended | leech`), `source` de l'exercice → `origin` (`claude | manual | inverse_auto`, pour ne pas confondre avec `Chapitre.source`), `affecteLePlanning` → `affectsScheduling`, `confiance` → `confidence`. Les libellés visibles restent en français.
2. **`attempts` remplacé par `reviewLogs`** (et non conservé à côté) : une seule source de vérité pour les stats, l'undo et la migration FSRS. La table `attempts` est supprimée par la version 3 de Dexie après conversion de chaque ligne. Les lignes historiques reçoivent `affectsScheduling: true` — elles ont réellement déplacé le planning SM-2 à l'époque, et le rejeu du journal doit reproduire ce qui s'est passé.
3. **Notes → ratings** : `again/hard/good/easy` → 1/2/3/4 (échelle FSRS). Une ligne sans note lisible prend 3 si juste, 1 si faux.
4. **Identifiants du journal** : UUID (chaîne) plutôt que l'auto-incrément de `attempts`, pour que l'import d'une sauvegarde soit idempotent (`bulkPut` par id) — c'est ce qui corrige les doublons d'attempts à l'import.
5. **Versionnage de la sauvegarde** : champ `schemaVersion` (autoritaire) + `version` conservé pour les anciens lecteurs. `migrateBackup()` est une fonction pure (`lib/migrations.ts`) partagée par l'import JSON et testée ; elle refuse les fichiers d'un schéma plus récent et tolère les tableaux manquants. Les hooks `upgrade` de Dexie réutilisent les mêmes fonctions (`attemptToReviewLog`, `upgradeExercise`).
6. **Statuts appliqués dès maintenant** : `loadScopeExercises` et les compteurs « dus » ne considèrent que `status === 'active'`. Tous les exercices existants sont migrés en `active`, donc aucun changement visible ; la file « à valider » (phase 2) n'aura qu'à insérer en `pending`.
7. **S'entraîner et Chrono ne déplacent plus le planning** (`affectsScheduling: false`) dès cette phase : le champ existe dans le journal et y écrire `true` pour ces modes aurait été faux. C'est la règle 3.4 appliquée par anticipation ; l'option « affecter quand même le planning » viendra avec la phase 3.
8. **Points de cours** : table `points` (`id, chapitreId, cahierId, anchor ≤ 200 caractères, title, nature, order`). Les exercices existants ont `pointId: null` ; ils seront rattachés lors de la prochaine génération (phase 2) ou manuellement.
9. **Tests** : vitest + `fake-indexeddb` ; `createDb(name)` exporté pour ouvrir des bases jetables. La migration Dexie est testée en écrivant réellement une base au schéma v2 puis en l'ouvrant avec le schéma v3.
10. **Image occlusion** : différée (P2). Les fiches sont du texte seul, aucune image n'est stockée — à reconsidérer après la phase 9 (stockage d'images en Blob).
11. **Git** : dépôt initialisé à cette phase (le projet n'en avait pas). Un commit par phase.

## Phase 1 — FSRS

1. **Bibliothèque** : `ts-fsrs` 5.4 (FSRS-6, paramètres par défaut, pas d'optimiseur côté client). Un seul module lui parle (`lib/fsrs.ts`) ; le reste manipule `FsrsCard`, une copie sérialisable de `Card` (dates en epoch ms) stockée dans `exercise.fsrs`. Le champ SM-2 `srs` disparaît (schéma Dexie v4, sauvegarde `schemaVersion: 4`).
2. **Migration SM-2 → FSRS** : (a) si un journal existe pour l'exercice, on rejoue chaque réponse (`next()` en boucle, sans fuzz — même calcul que `reschedule()`, reproductible) pour obtenir stabilité, difficulté, reps, lapses, état ; (b) sinon, conversion approximative documentée dans `fromSm2` : stabilité ≈ intervalle SM-2 (plancher 0,1 j), difficulté = 5 + (2,5 − ease) × 4,17 bornée à 1–10, état Review (Relearning si l'échec SM-2 était en cours, New si jamais vu). **Dans les deux cas la date d'échéance SM-2 est conservée** : rien ne devient dû ni reporté par le changement d'algorithme (transition graduelle, comme Anki).
3. **Paramètres exposés** : rétention visée 0,80–0,95 (défaut 0,90, curseur avec estimation « ≈ N révisions/jour » = Σ 1/intervalle_suivant(S, r) sur les cartes en état Review), intervalle maximal (défaut 365 j), nouveaux/jour (20), révisions max/jour (200, par cahier avec surcharge dans « Modifier le cahier »), jours légers, seuil leech (8, utilisé en phase 3), demande de confiance (phase 5). Fixés dans le code : `enable_fuzz: true`, `learning_steps ['1m','10m']`, `relearning_steps ['10m']`.
4. **Notes → ratings** pour les types auto-corrigés : faux → Encore, juste → Bien, « facile » explicite → Facile. « Juste avec indice → Difficile » attend un indice utilisable en session (aucun joueur n'en montre avant la réponse aujourd'hui) — à appliquer avec les nouveaux types (phase 3).
5. **Undo** : chaque `ReviewLog` porte `fsrsLog = { prev, next, log }`. Annuler restaure `prev` tel quel et supprime le journal — exact par construction ; un test vérifie que `rollback()` de ts-fsrs donne le même état mémoire. Raccourci `Ctrl+Z` et bouton dans l'en-tête de session ; l'annulation retire aussi la copie remise en fin de file après un « Encore ».
6. **File de révision et limites** : cartes en apprentissage d'abord (toujours servies, hors limites), puis cartes Review dues par échéance (plafond révisions/jour), puis nouvelles par date de création (plafond nouveaux/jour). Les compteurs du jour se lisent dans le journal (état de la carte *avant* la réponse : New → nouveau, Review → révision). Le plafond par cahier s'applique aussi en scope « tous les cahiers ». L'ancien plafond fixe de 50 disparaît. Le tableau de bord affiche ce que la session contiendrait réellement (« 37 dus · ~12 min », temps = médiane des durées du journal, 20 s par défaut).
7. **Jours légers** : après calcul FSRS, si l'échéance tombe un jour léger et que l'intervalle est ≥ 3 jours, on décale de ±1 puis ±2, ±3 jours (plus tôt d'abord : c'est la rétention qui perd le moins), jamais avant demain. L'état mémoire n'est pas touché. Les échéances déjà fixées ne bougent pas.
8. **Reporter / Avancer** (menu « … » du cahier) : reporter = cartes dues en état Review triées par récupérabilité décroissante, décalées de max(1 j, 5 % de l'intervalle) — règle du helper FSRS d'Anki ; avancer = cartes non dues triées par récupérabilité croissante, rendues dues maintenant. Seul `due` change ; l'impact affiché = probabilité de rappel moyenne avant/après.
9. **Modes hors planning** : S'entraîner et Chrono ne passent pas par FSRS (`affectsScheduling: false`), l'écran de fin le rappelle. Les réponses « Encore » en S'entraîner re-proposent l'exercice en fin de session sans le replanifier.
10. **Export du journal** : CSV au format fsrs4anki (`card_id, review_time, review_rating, review_state, review_duration`), uniquement les réponses ayant affecté le planning ; `review_state` = état avant la réponse (0 pour les lignes SM-2 sans détail).

## Phase 2 — Rendu, génération de qualité, validation, couverture

1. **Rendu** : `marked` (GFM, `breaks: true` pour que les retours à la ligne des textes collés survivent) + **KaTeX** (`$…$`, `$$…$$`, `\(…\)`, `\[…\]`, `\$` littéral, extension mhchem) + DOMPurify. Les formules sont extraites *avant* markdown (sinon `_` et `*` des formules sont interprétés) et réinjectées après. Un seul composant `<Markdown>` (bloc ou `inline`) utilisé dans les fiches, compléments, joueurs, résultats, cartes d'exercice, validation. **Cartes mentales** : texte SVG brut, le LaTeX y reste tel quel (P2 : foreignObject casserait l'export PNG).
2. **Prompt à deux temps, une seule réponse JSON** : étape 1 = liste des points de cours (`id`, `title`, `nature`, `anchor` = citation exacte ≤ 200 caractères), étape 2 = 1 à 3 exercices par point avec `pointId`. Schéma inclus dans le prompt. Règles d'écriture reprises telles quelles de la spécification 3.6 (un fait par exercice, questions autonomes, un seul trou, distracteurs compétitifs avec `distractorReasons`, `correctedStatement` obligatoire en V/F, LaTeX). Les types `demonstration` et `rappel_libre` n'entrent dans le prompt qu'avec leurs joueurs (phase 3).
3. **Génération ciblée** (`focus`) : le même prompt peut recevoir des points existants (ids réels réutilisés) et/ou des passages sans point (points créés). Utilisé par la vue couverture et par « Générer les exercices de ce complément ».
4. **Parseur** : zod ; `pointId` local ou réel ; alias `enonceCorrige` accepté ; un exercice sans `pointId` mais avec `anchor` identique à celle d'un point y est rattaché. À l'import, un point dont l'ancre existe déjà dans la fiche est réutilisé plutôt que dupliqué.
5. **Linter déterministe** (`lib/lint.ts`) : réponse > 25 mots, double question (deux interrogatifs reliés par « et », ou deux « ? »), énumération (« cite / énumère / quels sont… »), pronom initial, trou sur mot-outil, réponse visible dans la phrase, plusieurs trous (info), choix QCM > 2× la médiane, bonne réponse toujours à la même position (≥ 3 QCM du lot), distracteurs non justifiés (info), V/F sans énoncé corrigé, LaTeX déséquilibré, ancre introuvable (points), quasi-doublon. Test : 10 défauts semés → 10 détectés (critère ≥ 9).
6. **Dédoublonnage** : trigrammes de caractères sur texte normalisé (minuscules, sans accents ni ponctuation), coefficient de Dice, seuil 0,8, contre les exercices existants de la fiche et les éléments précédents du lot. Signalé, jamais supprimé.
7. **File « à valider »** : les exercices générés arrivent en `status: 'pending'` (hors planning) et la page `/…/valider` les présente un par un avec les défauts (J garder, K ignorer, E modifier, Ctrl+A tout garder). Réglage « Toujours tout garder sans valider » (`autoValidate`) pour activer directement. La liste des exercices d'une fiche montre un badge « À valider » et une bannière avec le compte.
8. **Couverture** : la fiche est découpée en blocs (paragraphes, puces ; les titres seuls ne comptent pas, les tableaux sont ignorés, blocs < 4 mots ignorés). Un bloc est ancré si l'ancre d'un point le recouvre (inclusion normalisée, ou fenêtre de 30 caractères pour une citation partielle). Affichés : points sans exercice actif/en attente, blocs sans point, pourcentage ancré, bouton « Générer pour ces points ».
9. **Compléments** : les compléments gardés restent listés avec « Générer les exercices de ce complément » (génération ciblée sur le texte du complément).
10. **Édition** : modal d'édition par type (choix/bonnes réponses/raisons pour les QCM, énoncé corrigé pour les V/F, paires « terme | définition », etc.), aperçu rendu et linter en direct ; accessible depuis la validation (E) et la carte d'exercice. Suspendre / réactiver depuis la carte.

## Phase 3 — Nouveaux types, sessions, leeches

1. **Rappel libre guidé** (`rappel_libre`) : un seul par fiche, généré avec une `checklist` de 5 à 15 notions (chaque item peut porter le `pointId` de son point de cours, mappé à l'import). Joueur en deux temps : écriture libre (chrono affiché, Ctrl+Entrée), puis cases à cocher « ce que tu avais écrit » + case « sans hésitation » (activée seulement si tout est coché). Note : < 50 % Encore, 50–80 % Difficile, ≥ 80 % Bien, 100 % sans hésitation Facile. Les notions non cochées relancent leurs exercices : ceux du même `pointId`, actifs et non dus, deviennent dus immédiatement (compte affiché dans le résumé).
2. **Démonstration / méthode** (`demonstration`) : `steps[{text, why}]`, `statement`, `title`. Estompage stocké sur l'exercice (`fading: {level, streak}`) : niveau 1 = une étape masquée (choisie par un hachage id + reps, donc différente à chaque révision), niveau 2 = une étape sur deux, niveau 3 = énoncé seul avec zone de reconstitution puis comparaison. Auto-noté 1–4 après révélation ; deux réussites consécutives montent d'un niveau, « Encore » redescend, « Difficile » remet la série à zéro. « Pourquoi ? » est un bouton facultatif par étape, jamais imposé (Barbieri 2023).
3. **Vrai/Faux + correction** : choisir « Faux » exige d'écrire l'énoncé corrigé avant la révélation (quand `correctedStatement` existe). Le verdict est auto-corrigé ; une correction juste mais éloignée de l'attendu (Dice < 0,45 sur trigrammes) est notée « Difficile » et signalée. Le prompt n'émet plus de V/F sans `correctedStatement`.
4. **Entrelacement** (`lib/interleave.ts`) : la file Réviser (après limites) est réordonnée gloutonnement — jamais deux exercices du même point à la suite (pénalité −100), fiche différente +2, type différent +1, ordre d'échéance en départage ; cartes en apprentissage d'abord. Cahiers marqués « vocabulaire / lexique » (`Cahier.lexical`, case dans « Modifier le cahier ») : bloc par fiche, ordre d'échéance, en fin de file. Test : session mixte sur deux fiches sans siblings adjacents.
5. **Siblings enterrés** : après une réponse en Réviser (réglage `burySiblings`, défaut oui), les autres exercices actifs du même point, dus et pas en apprentissage, passent à demain 0 h et sortent de la file courante. Comptés dans le résumé.
6. **Feedback** : immédiat en Réviser et S'entraîner ; **différé en Chrono** (`deferFeedback` : les joueurs répondent sans verdict, l'écran de fin liste chaque question avec juste/faux, réponse attendue, énoncé corrigé, explication). Jamais absent.
7. **Leeches** : au moment où une réponse « Encore » porte `lapses` au seuil (`leechThreshold`, défaut 8), l'exercice passe en `leech` (hors planning) et une fenêtre propose *Réécrire avec Claude* (prompt « principe d'information minimale », 1 à 3 exercices atomiques importés en `pending`, le leech est suspendu pour garder son historique), *Suspendre* ou *Continuer*. Test : leech déclenché au 8ᵉ échec.
8. **Session** : `E` ouvre l'éditeur sur l'exercice courant (la file recharge la copie modifiée), `-` enterre l'exercice à demain sans répondre (Réviser), `@` le suspend (confirmation), `?` affiche l'aide des raccourcis, `Ctrl+Z` annule. Résumé : réussite, temps, exercices reportés, exercices relancés, prochain rappel de la sélection, « À retravailler » (hors Chrono, qui affiche la correction complète à la place).
9. **« Juste avec indice → Difficile »** : aucun joueur ne montre d'indice avant la réponse (les flashcards affichent l'indice avec la réponse) ; la règle reste inappliquée, documentée ici.

## Phase 4 — Préparation d'examen

1. **Stockage** : les examens vivent sur le cahier (`Cahier.examens[]`, comme le suggère la spécification) : `name`, `date` (minuit local du jour J), `chapitreIds`, `boostFromDays` (défaut 14), `sessions[3]` (`at`, `done?`), `archived`. Pas de table dédiée : quelques examens par cahier, lus avec le cahier.
2. **Plan de réapprentissage successif** : trois séances qui se terminent la veille de l'examen, avec des écarts qui se resserrent (Cepeda : l'écart optimal est une fraction du délai restant) : S3 = J−1, S2 = S3 − max(1, 25 % du délai), S1 = S2 − max(1, 30 % du délai). Pour 21 jours : J−12, J−6, J−1 (l'exemple de la spécification). Jamais avant aujourd'hui ; jours dédoublonnés quand le délai est court. Changer la date replanifie en conservant les séances déjà faites.
3. **Séance d'examen** (`mode=exam&session=i`) : tous les exercices actifs des fiches de l'examen, entrelacés ; chaque échec remet l'exercice en fin de file jusqu'à un rappel correct ; la séance est cochée à la fin. Ces réponses **affectent le planning** (`affectsScheduling: true`) : ce sont de vraies récupérations avec feedback, FSRS doit en tenir compte — contrairement au cramming.
4. **Surcharges du planificateur** : pour chaque fiche d'un examen à venir, `maximum_interval` = min(global, ⌊jours restants / 2⌋, ≥ 1) et rétention visée 0,95 à partir de `boostFromDays` avant l'examen. Un planificateur ts-fsrs par couple (rétention, intervalle max) est mis en cache dans le contexte de session (`schedulerFor(exercise)`), utilisé pour les réponses, les intervalles affichés et l'ordre du cramming. Quand plusieurs examens couvrent une fiche, le plus strict gagne. Nuance : ts-fsrs impose Difficile < Bien < Facile strictement, si bien qu'un « Bien » ou un « Facile » peut dépasser le plafond d'un ou deux jours.
5. **Cramming** (« Réviser tout maintenant ») : tous les exercices actifs des fiches de l'examen triés par récupérabilité croissante, feedback immédiat, `affectsScheduling: false` — aucune échéance ne bouge (test). Le résumé le rappelle.
6. **Après l'examen** : les surcharges cessent automatiquement dès que la date est passée (calculées à chaque chargement) ; l'examen reste affiché avec un message et un bouton « Archiver » qui le masque — c'est le « proposer de ramener la rétention au réglage global » de la spécification, sans action irréversible.
7. **Tableau de bord** : jusqu'à trois examens à venir avec compte à rebours, jauge des séances et bouton vers la prochaine séance (« en retard » si sa date est passée).

## Phase 5 — Confiance, calibration, statistiques, série

1. **Confiance avant la réponse** : sélecteur Sûr / Hésitant / Aucune idée (S, H, A) affiché *avant* la révélation sur les flashcards, démonstrations et textes à trous (Nelson & Dunlosky : un jugement après lecture est sans valeur). Facultatif (on peut révéler sans choisir), désactivable (`askConfidence`), jamais en Chrono. Stocké dans `ReviewLog.confidence`.
2. **Hypercorrection** : une erreur donnée avec « sûr » en mode planifié ajoute `forcedDue = [J+1, J+7]` (minuit local) sur l'exercice ; à chaque réponse, les dates passées sont retirées et la prochaine échéance FSRS est plafonnée par la première date restante. Le pas de réapprentissage (minutes) reste prioritaire quand il est plus proche. Le résumé de session compte ces erreurs.
3. **Calibration** : par niveau de confiance, réponses et taux de réussite sur 90 jours ; avertissement quand « sûr » réussit sous 85 % avec au moins 10 réponses (avant, pas de verdict : trop peu de données).
4. **Statistiques** (`/stats`), toutes recalculées depuis le journal + les exercices (`lib/stats.ts`, testé) : *true retention* = réussite des réponses planifiées sur des cartes déjà en état Review (ni apprentissage, ni nouvelles, ni entraînement libre) sur 7/30/90 jours et par cahier ; prévision de charge à 30 jours (retard cumulé sur aujourd'hui) ; heatmap 365 jours ; réussite par heure (réponses planifiées) ; rétention estimée = moyenne des récupérabilités des cartes actives non nouvelles, avec le planificateur de chaque fiche (surcharges d'examen incluses).
5. **Série bienveillante** : un jour compte s'il atteint l'*objectif minimal* (`minimalGoal`, défaut 10 réponses, tous modes), distinct de l'*objectif du jour* (`dailyGoal`, indicatif) ; jusqu'à 2 jours manqués par mois civil sont « gelés » automatiquement sans casser la série ; aujourd'hui ne casse jamais la série ; à 0 le tableau de bord dit « prêt à commencer », jamais « série perdue ». Remplace l'ancienne série stricte (`computeStreak`).

## Phase 6 — Carte mentale à trous, saisie, cartes inverses

1. **Exercices de carte mentale** : à l'enregistrement d'une carte de fiche, deux exercices `carte_trous` sont créés (variantes `trous` et `reconstruction`, actifs, sans point de cours) ; ils suivent le planning FSRS comme les autres et sont supprimés avec la carte. Les cartes de synthèse d'un cahier (sans fiche) n'en ont pas : ce sont des vues d'ensemble. La page de la carte reste consultable mais dit explicitement que lire n'est pas réviser et renvoie vers l'entraînement.
2. **Rendu du joueur** : la carte est jouée sous forme de *plan indenté* (une ligne par nœud, pastille de couleur par branche), pas dans le SVG : le masquage et la saisie y sont plus simples et accessibles au clavier, et la structure reste lisible. Le rendu SVG masqué est possible plus tard.
3. **Masquage** : 30–50 % des nœuds non racine, choix déterministe à partir de l'id de l'exercice et du nombre de révisions (le masque change à chaque passage), au moins un nœud visible par branche. Verdict Su / Raté par nœud révélé ; note par ratio comme le rappel libre. Reconstruction : racine et branches de niveau 1 visibles, sous-nœuds saisis de mémoire par branche, puis comparaison par cases à cocher.
4. **Saisie de la réponse** : `typed` sur une flashcard (Claude le pose pour les formules et valeurs exactes ; réglage « toujours saisir » pour tout) ; comparaison Dice sur texte normalisé ou sur LaTeX normalisé (délimiteurs, `\left/\right`, espaces fines, accolades simples, `\frac` → `(a)/(b)`, `\cdot` → `*`), seuil 0,85 pour proposer « Bien », sinon « Encore » proposé ; diff mot à mot affiché ; l'élève tranche toujours (les quatre boutons restent disponibles, le bouton suggéré est mis en avant). L'ordre des facteurs commutatifs n'est pas normalisé (aide, pas juge).
5. **Cartes inverses** : à l'import (réglage `autoInverse`, défaut oui), chaque flashcard d'un point de nature `definition` ou `formule` dont la réponse fait ≤ 12 mots produit sa carte inverse (question ↔ réponse, `inverse: true`, `origin: 'inverse_auto'`, même point, même statut d'import donc « à valider »). Une seule inverse par réponse identique. Le prompt demande à Claude de ne plus les écrire lui-même.

## Phase 7 — Persistance et portabilité

1. **PWA** : `vite-plugin-pwa` (`registerType: 'autoUpdate'`), manifest en français, icônes SVG (`sizes: any`, acceptées par Chromium pour l'installation ; pas d'outil de rasterisation dans le projet), précache Workbox élargi aux `.woff2` et `.wasm` (KaTeX, sql.js) avec un plafond de 6 Mo par fichier.
2. **Persistance** : `navigator.storage.persist()` est demandé à la création du premier cahier (dès qu'il y a quelque chose à perdre) et depuis les réglages ; l'état (`persisted()`, usage / quota) est affiché. Safari est détecté par l'agent utilisateur pour afficher l'avertissement des 7 jours.
3. **Sauvegarde automatique** : File System Access API (Chrome / Edge) ; le `FileSystemFileHandle` est stocké dans une nouvelle table `kv` (schéma Dexie v5) avec l'état (nom, dernière écriture, erreur). Déclenchement par `Dexie.on('storagemutated')` (toutes les écritures, cet onglet ou un autre) avec un délai de 2,5 s, en ignorant les écritures dans `kv` elle-même. Après un rechargement, l'autorisation d'écriture redevient « prompt » : un bouton « Reprendre » la redemande (geste utilisateur obligatoire). Hors Chromium : export manuel.
4. **Conflits à la restauration** : si la base contient des modifications plus récentes que `exportedAt` du fichier, une confirmation explique que la fusion fera reprendre leur ancienne version aux éléments présents dans le fichier.
5. **Export Anki** : `.apkg` écrit sans bibliothèque tierce dédiée — sql.js (SQLite en wasm) + JSZip, schéma legacy v11 comme genanki (`genanki-js` est pensé pour des balises `<script>` globales et n'est pas maintenu côté npm). Notetypes « Cahiers Basic » et « Cahiers Cloze », un paquet par fiche (`Cahiers::Matière::Fiche`), GUID déterministe par exercice (ré-export = mise à jour), LaTeX converti en `\(…\)`. L'historique FSRS n'est pas transféré. Vérifié par relecture du fichier produit dans les tests, pas par import dans Anki desktop.
6. **CSV / TSV** : recto, verso, type, tags, fiche, cahier, statut.

## Phase 8 (optionnelle) — QCM pondéré, pré-test

1. **QCM pondéré par la confiance** : réglage `weightedMcq`, désactivé par défaut (une seule étude, Sparck 2016). Sur un QCM à réponse unique hors Chrono : premier choix = réponse principale, second choix facultatif = alternative, curseur 50–95 % ; score = confiance placée sur la bonne réponse (1 si un seul choix juste, 0 si aucune des deux) ; note : ≥ 75 % Bien, ≥ 50 % Difficile, sinon Encore ; le score est affiché dans le feedback. Jamais de note de confiance après la révélation (inefficace).
2. **Pré-test** : menu « … » du cahier → « Pré-test d'un chapitre » : Claude génère 3 à 5 questions conceptuelles à réponse courte à partir du programme (ou du programme standard du niveau) ; l'élève répond sans notation, les réponses attendues n'apparaissent qu'à la fin. Les questions sont mémorisées (`kv`, une par cahier) et, à la prochaine génération d'exercices dans ce cahier, une case cochée par défaut les ajoute comme passages à couvrir (seuls les items pré-testés profitent de l'effet, g ≈ 0,54 contre 0,04 pour le reste), puis le pré-test est effacé.
3. **Occlusion d'image** : non implémentée — les fiches restent du texte, aucune image n'est stockée (voir phase 0). À reconsidérer avec le stockage d'images.

## Revue 1 — Corrections après relecture de ce journal

### 0. Filet de sécurité
- Tests (112) et build verts avant toute modification.
- La table `kv` est créée dès le schéma v3 (et non v5) ; la version 5 reste déclarée, vide, pour que les bases déjà en v5 s'ouvrent. Les hooks `upgrade` v3 et v4 copient d'abord le contenu précédent, tel quel, dans `kv` sous `backup_before_v3` / `backup_before_v4` (`{ app, version, schemaVersion, exportedAt, migrationBackup: true, <tables> }`, `settings` en objet comme `exportBackup`). Le hook v5 ne transforme rien : pas de copie. Une base neuve ne passe jamais par un hook : rien n'est écrit inutilement.
- Réglages → Données → « Sauvegardes de migration » : liste avec date et taille, téléchargement ; ces fichiers se restaurent par « Restaurer une sauvegarde » (`migrateBackup` comprend toutes les formes passées).
- Test : une base v2 réelle ouverte avec le schéma courant contient `backup_before_v3` (attempts et `srs` intacts) et `backup_before_v4` (reviewLogs, `srs`) ; `migrateBackup()` sur la copie v2 donne les mêmes états FSRS (due, état, reps, stabilité) et les mêmes notes de journal que la base migrée ; la copie se réimporte dans une base neuve.

### 1. Saisie de formules
- `isFormula(réponse)` : délimiteurs `$`, `\(`, `\[`, commande LaTeX, ou symboles au moins aussi nombreux que les lettres (E = mc^2, 9,81 m/s²).
- Formule : « Bien » suggéré uniquement en cas d'égalité exacte après normalisation ; sinon aucune suggestion (quatre boutons au même niveau, aucun focus imposé) et un diff caractère par caractère sur le LaTeX normalisé (ajouts barrés en rouge, manques en vert). Le score Dice n'est plus qu'informatif pour les formules.
- Texte : ≥ 0,85 → « Bien » suggéré ; 0,6–0,85 → aucune suggestion (message « à toi de juger ») ; < 0,6 → « Encore » suggéré.
- Normalisation LaTeX complétée : `\displaystyle`/`\textstyle`, `\,` `\;` `\!` `\:`, `\dfrac`/`\tfrac` → `\frac` (désormais **avant** la réécriture de `\frac`, ce qui était un ordre de règles fautif), `\times` → `\cdot`, `^{x}` → `^x` et `{x}` → `x` pour un seul caractère hors nom de commande. L'ordre des facteurs n'est pas normalisé.
- Tests : `E = -mc^2` contre `E = mc^2` → aucune suggestion, diff sur le signe ; `\frac{1}{2}mv^2` contre `\dfrac{1}{2} m v^{2}` → exact ; `x^2` contre `x^3` → diff sur l'exposant ; une formule longue à un signe près dépasse 0,85 de similarité et n'est pourtant pas suggérée.

### 2. Vrai/Faux + correction
- `lib/truefalse.ts` (pur, testé) : le verdict reste auto-corrigé (« Vrai » sur un énoncé faux = Encore, bouton Continuer seul) ; avec un verdict juste et une correction écrite, la note n'est plus imposée : « Bien » proposé si Dice ≥ 0,45, « Difficile » sinon, mais les quatre boutons (1–4) restent actifs, le proposé est mis en avant. Énoncé attendu et énoncé de l'élève affichés côte à côte, avec l'explication.
- Test : une correction juste mais paraphrasée reçoit « Difficile » en suggestion et peut être notée « Bien ».

### 3. Exercices de carte mentale
- Régénération : `saveMindmap` réutilisait déjà l'id de la carte précédente, donc les deux exercices `carte_trous` (même `id`, même état FSRS, même journal) n'étaient pas touchés ; la phase 6 le décrivait mal (« supprimés avec la carte » ne vaut que pour la suppression explicite). Comportement confirmé par un test : régénérer conserve `exercise.id`, `fsrs.stability`, `reps`, le statut et les `reviewLogs`.
- Suppression explicite : la confirmation mentionne désormais la perte des deux exercices et de leur historique, et conseille de régénérer plutôt que supprimer.
- Nouveau réglage `mindmapExercisesActive` (défaut : non) : les exercices de carte sont créés « à valider » ; on les active depuis la file de validation ou en cochant le réglage. Test : défaut → `pending`, réglage coché → `active`.

### 4. Linter : précision et rappel mesurés
- Fixture `tests/fixtures/lot-physique.json` : 40 exercices d'électrostatique (PC) au format de sortie du prompt, avec 8 points ancrés dans une fiche, annotés `expectedIssues` (19 défauts sur 17 exercices, tous les codes bloquants représentés, une ancre paraphrasée). Test `lint.fixture.test.ts` : précision et rappel calculés sur les paires (exercice, code) de sévérité `warn`, échec sous 0,8. Fixture `tests/fixtures/lot-maths-corrects.json` : 15 exercices de maths corrects (`\frac`, `\sum`, normes `\|`, « et » non interrogatif) → 0 défaut `warn` exigé.
- Bug révélé par la fixture (première passe : précision 0,33, rappel 0,29) : le parseur de trous refusait toute accolade dans un `{{…}}`, donc les clozes de formule `{{$\dfrac{1}{2}mv^2$}}` demandés par le prompt étaient rejetés à l'import (« Texte à trous sans {{trou}} »), silencieusement pour l'utilisateur. `lib/cloze.ts` équilibre maintenant les accolades à la main (`findBlanks`, `replaceBlanks`, `blankAnswers`) et ne coupe plus les variantes sur un `|` échappé (`\|` des normes). Tests `cloze.test.ts`.
- Règle durcie : `cloze_visible` compare aussi les réponses de plusieurs mots au singulier (« volts par mètre » caché, « volt par mètre » visible), les réponses d'un seul mot restent en égalité stricte (« nul » n'est pas trahi par « non nuls »). Aucune règle assouplie.
- Chiffres réels après corrections : précision 1,00, rappel 1,00 sur la fixture physique (un cloze annoté au départ `latex_unbalanced` seul fuyait aussi sa réponse dans la phrase : l'annotation a été complétée plutôt que la règle affaiblie) ; 0 défaut bloquant sur la fixture maths (une paire Vrai/Faux à énoncés quasi identiques, signalée à juste titre comme doublon, a été remplacée par un énoncé distinct).

### 5. Export `.apkg` : vérification réelle
- `scripts/verify-apkg.mjs` (`npm run verify:apkg -- fichier.apkg`, `--json`, `--verbose`) : ouvre le zip, lit `collection.anki2` avec sql.js et contrôle : un seul enregistrement `col` (`ver` 11), `models`/`decks`/`conf`/`dconf`/`tags` JSON parsables, clés Anki des modèles (`id, name, type, flds, tmpls, css, mod, usn, sortf, did, latexPre, latexPost, req`) et des decks (`id, name, mod, usn, desc, conf, dyn, collapsed, extendNew, extendRev`), `ord` des champs et templates, parents des decks `A::B::C` présents, `notes.sfld` renseigné et égal au champ de tri dépouillé du HTML, `notes.csum` = 8 premiers hexadécimaux de SHA-1 du champ dépouillé, champs séparés par `\x1f`, guid unique, tags entourés d'espaces, `{{cN::…}}` dans les notes cloze, `cards.ord` valide (template existant, ou numéro de cloze présent dans la note), deck de chaque carte existant, au moins une carte par note, `media` présent (`{}`). 162 contrôles sur le paquet de test, 0 échec. Test `apkg.verify.test.ts` : construit un paquet avec un exercice de chaque type exporté (flashcard, cloze à deux trous dont un `{{$…$}}`, QCM converti, V/F, association, ordre, démonstration, rappel libre), l'écrit dans un dossier temporaire et exécute le script en sous-processus ; un zip sans `collection.anki2` doit sortir avec le code 1.
- Comparaison avec genanki (sources `note.py`, `model.py`, `deck.py`, `card.py`, `package.py`, `apkg_col.py`, `util.py` lues) et alignements faits : `id` du modèle sérialisé en chaîne, `bfont: ''` et `bsize: 0` ajoutés aux templates, `extendNew` 10 sur le deck Default, GUID = base91 des 64 premiers bits de **SHA-256** (c'était SHA-1 : les paquets exportés avant cette revue ré-importés créeront des notes neuves plutôt que de mettre à jour ; aucun utilisateur concerné à ce stade). Repli sha256 pur JS pour les contextes sans WebCrypto (PWA ouverte hors https).
- Écarts conservés volontairement (Anki lui-même fait ainsi, et le lot demande explicitement `csum` = SHA-1) : genanki écrit `csum = 0` et `sfld` non dépouillé, nous écrivons le checksum réel et le champ dépouillé ; genanki écrit `due = 0` pour toutes les cartes, nous écrivons une position croissante ; `crt` = aujourd'hui 4 h (genanki : constante 2014). GUID calculé sur l'id de l'exercice et non sur les champs joints par `__` comme genanki, pour qu'une correction de texte mette à jour la note Anki au lieu d'en créer une seconde.
- Hors périmètre découvert : `process.exit()` après sql.js fait planter Node sur Windows (assertion libuv `UV_HANDLE_CLOSING`) ; le script positionne `process.exitCode` à la place.
- **Import dans Anki desktop : reste à confirmer manuellement par l'utilisateur.** Procédure : 1) Réglages → Données → « Exporter pour Anki (.apkg) » ; 2) dans Anki, Fichier → Importer… → choisir le `.apkg`, laisser « Mettre à jour les notes existantes » ; 3) vérifier dans le navigateur de cartes que les decks `Cahiers::…` apparaissent, qu'une carte cloze affiche le trou et qu'une formule `\(…\)` est rendue par MathJax (Outils → Vérifier la base de données ne doit rien signaler).

### 6. Estimation « ≈ N révisions/jour » par simulation
- `simulateReviewsPerDay` (`lib/fsrs.ts`) remplace la somme des inverses d'intervalles : chaque carte active hors état « nouvelle » rejoue 90 jours avec FSRS (fuzz désactivé), réponse « Bien » avec probabilité = rétention visée, « Encore » sinon, tirage FNV-1a de `id:indexDeRévision` (reproductible entre deux rendus) ; les cartes en retard sont comptées au jour 0, les étapes de réapprentissage du même jour comptent comme révisions ; résultat = moyenne des 30 derniers jours simulés. Au-delà de 500 cartes : échantillon déterministe (ids au hachage le plus bas) et extrapolation proportionnelle.
- Budget : 2 000 cartes → 500 simulées en ≈ 90 ms sous vitest (test qui échoue au-delà de 200 ms) ; le calcul est mémoïsé dans Réglages et ne tourne qu'au changement du curseur ou des cartes. L'ancien calcul est supprimé.
- Les cartes nouvelles sont exclues (leur charge dépend de la limite « nouvelles/jour », pas de la rétention) ; le libellé du réglage le précise.
- Test : rétention 0,95 > 0,85 ; résultat déterministe ; simulation inférieure à l'ancienne somme stationnaire sur les mêmes cartes (elle surestimait bien) ; échantillonnage et extrapolation à ±30 % du calcul complet.

### 7. Deux oublis vérifiés
- Prompts « Rédiger avec Claude » et « Compléter » : **aucun des deux n'exigeait le LaTeX** (seul le prompt d'exercices avait sa règle 7). Ajout d'une règle commune `LATEX_RULE` (formules, grandeurs avec unité via `\mathrm`, équations-bilans via `\ce{…}`, avec exemples : $g = 9{,}8\ \mathrm{m\cdot s^{-2}}$, $\ce{2H2 + O2 -> 2H2O}$, interdiction du texte brut « 9,8 m/s² ») en règle 6 des deux prompts (la règle « programme » de la rédaction passe en 7), exemple JSON de sortie avec du LaTeX et rappel que les antislashs sont doublés dans le JSON. Test `prompt.test.ts`.
- Rendu : test `importClaude.test.ts` qui fait passer une fiche au format demandé (titres, `$…$`, `$$…$$`, `\mathrm`, `\ce`) par `parseFicheResponse` puis `renderMarkdown` : KaTeX rend chaque formule (`katex-display` pour le bloc), mhchem rend `\ce` sans `katex-error`. Hors périmètre découvert et corrigé : une réponse où Claude oublie de doubler un antislash (`"\dfrac"`) était rejetée comme « JSON invalide » ; `repairJson` double maintenant les antislashs qui ne forment pas une séquence d'échappement JSON valide (et garde `\"`, `\n`, `\uXXXX`, `\\`), pour tous les importeurs.
- `DESCRIPTIF.md` : il avait été mis à jour à chaque phase ; retouché plutôt que réécrit : mention « occlusion d'image non implémentée » retirée (rien de non implémenté n'est plus cité), sections saisie de formules, Vrai/Faux, exercices de carte mentale, estimation de charge, exports `.apkg` (script de vérification, import Anki à confirmer), sauvegardes de migration, LaTeX dans les prompts de rédaction / complément, liste des tests ; ligne « Revue 1 » dans l'historique.

### 8. Points mineurs
- **Icône iOS** : `public/apple-touch-icon.png` (180×180, opaque, fond plein comme l'exige Safari qui applique son propre arrondi) générée par `scripts/make-apple-touch-icon.mjs` (`npm run make:icon`) : le projet n'a ni sharp ni canvas, le script rastérise les trois formes du SVG (fond, contour de fiche, trois lignes) avec sur-échantillonnage et encode le PNG via zlib. Référencée par `<link rel="apple-touch-icon">` et dans le manifeste PWA (`sizes: 180x180`). Test : le script produit un PNG 8 bits couleur 180×180 et l'icône livrée est présente.
- **Autosave** : la taille du dernier JSON écrit est enregistrée avec l'état (`bytes`) et affichée dans Réglages → Sauvegarde automatique ; au-delà de 20 Mo, avertissement (réécriture complète à chaque modification, conseil d'export manuel et d'archivage). Pas de test unitaire : le code touche l'API File System Access ; l'écriture incrémentale reste hors périmètre.
- **Règle « juste avec indice → Difficile »** : non applicable, aucun joueur n'affiche d'indice avant la réponse (l'indice d'une flashcard n'apparaît qu'après la révélation, sous la réponse). À appliquer le jour où un bouton « Indice » est ajouté.

### Vérification finale (exécutée le 2026-09-12, après le commit de la section 8)
1. `npm test` et `npm run build` : **fait** — 22 fichiers, 146 tests verts ; build Vite + PWA vert (43 entrées pré-cachées).
2. Session Réviser : **fait** — vérifié dans le navigateur : les quatre boutons affichent leur intervalle (« Encore 10 min / Difficile 9 j / Bien 10 j / Facile 11 j » sur une carte de stabilité 5) ; après une réponse, le nombre d'exercices dus passe de 6 à 5, `Ctrl+Z` le ramène à 6 et le compteur de session revient à 1/6 (journal inchangé).
3. Fixture `lot-physique.json` : **fait** — précision 1,00 et rappel 1,00 (19 défauts attendus, 19 trouvés, 0 faux positif) ; seuil du test à 0,8 ; 0 défaut bloquant sur les 15 exercices de maths corrects. Chiffres détaillés en section 4 (dont la première passe à 0,33 / 0,29 qui a révélé le bug des trous de formule).
4. Examen à J+21 : **fait** — tests existants `exam.test.ts` (plan J−12 / J−6 / J−1) et `session.test.ts` (« cramming never moves a due date »), tous verts.
5. Saisie `E = -mc^2` contre `E = mc^2` : **fait** — test `typed.test.ts` (aucune suggestion, diff caractère) et vérification navigateur : bandeau « Formule différente : compare caractère par caractère », le signe `-` surligné dans le diff, aucun des quatre boutons mis en avant.
6. Régénération d'une carte mentale : **fait** — test `db.test.ts` : `id`, `fsrs.stability`, `reps`, statut et `reviewLogs` conservés ; suppression explicite = suppression des exercices, avec confirmation qui le dit.
7. `.apkg` : **fait** — `scripts/verify-apkg.mjs` : 162 contrôles, 0 échec sur le paquet de test (exécuté en sous-processus par `apkg.verify.test.ts`, et à la main). L'import dans Anki desktop reste **à confirmer manuellement** (procédure en section 5).
8. Sauvegarde de secours `backup_before_v3` : **fait** — test `db.test.ts` : base v2 ouverte en v5 → `backup_before_v3` et `backup_before_v4` présents dans `kv`, ré-importables, contenu migré identique à `migrateBackup()` ; téléchargement dans Réglages → « Sauvegardes de migration ».

Partiellement / non fait : rien dans la liste ci-dessus. Restent hors périmètre et notés : import Anki réel (à confirmer par l'utilisateur), écriture incrémentale de l'autosave, règle « indice → Difficile » (aucun indice avant la réponse).

## Revue 2 — Lot court après la Revue 1

### 0. Préalable
`npm test` (146 tests) et `npm run build` verts avant de commencer.

### 1. `repairJson` corrompait les commandes LaTeX (critique)
- Constat confirmé : la réparation de la Revue 1 ne touchait pas `\b \f \n \r \t`, valides en JSON ; `"\frac"` devenait form-feed + `rac`, `"\theta"` tabulation + `heta`, `"\nabla"` saut de ligne + `abla`, sans erreur. Pire : ce JSON étant valide, `JSON.parse` réussissait du premier coup et la réparation ne s'exécutait même pas.
- Nouveau module pur `lib/repairJson.ts`, scanner caractère par caractère (jamais de regex globale) qui ne travaille qu'à l'intérieur des chaînes JSON : (1) contexte mathématique suivi (`$…$`, `$$…$$`, `\(…\)`, `\[…\]`, délimiteurs à antislash simple ou double) → tout antislash simple suivi d'une lettre est une commande, y compris devant `b f n r t` ; (2) hors contexte : `\b \f \r \t` + lettre = commande, `\n` + lettre = commande seulement pour `nabla neq ne nu not notin newline nmid nparallel nrightarrow nexists` suivies d'un non-lettre, sinon saut de ligne ; `\\`, `\"`, `\/`, `\uXXXX` conservés ; tout autre caractère après un antislash (`\(`, `\{`, `\,`, `\|`, `\$`…) est doublé ; virgules finales retirées. La réparation s'exécute désormais **toujours** avant `JSON.parse` (un JSON correct ressort identique octet pour octet, test de non-régression).
- Jamais silencieux : `repairJson` renvoie `{ text, repairs: { doubledBackslashes, trailingCommas, commands, offsets } }` ; `runWithRepairs` expose le compte à l'écran d'import ; `ClaudeRoundTrip` affiche le bandeau « N antislashs réparés — vérifie les formules » avec les commandes concernées ; les offsets sont attribués aux éléments du tableau `exercises` (`locateArrayElements`) → `repaired: true` sur l'exercice, stocké en base (champ optionnel, pas de migration), badge « Antislashs réparés — vérifie les formules » et **tête de la file de validation**. Les éléments rejetés portent leur texte brut (`raw`) ; `RejectedList` les affiche avec la raison et « Copier les rejetés » produit le message prêt à recoller (raisons + blocs JSON, « renvoie-les corrigés au même format »).
- Prompts : `JSON_LATEX_NOTE` donne l'exemple explicite `"\\frac{1}{2}"` et NON `"\frac{1}{2}"` avec la raison, et demande un seul bloc ```json ; ajouté aux prompts exercices (règle 7), leech, compléments, rédaction, carte mentale et pré-test (exemple de formule dans le schéma de sortie).
- Tests `repairJson.test.ts` (23) : les 13 commandes en contexte `$…$`, délimiteurs `\(` `$$` `\[`, compteur exact, `"a\nb"` conservé, `\theta` / `\frac` / `\begin` / `\right` / `\tau` hors contexte, `\n` + commande connue, non-régression octet à octet, virgules finales, attribution aux éléments. `importClaude.test.ts` : compte remonté sur les résultats, `repaired` sur le bon exercice, `raw` des rejetés.
- Vérifié dans le navigateur : bandeau « 4 antislashs réparés » (\eta, \frac, \theta, \ce), liste des rejetés avec texte brut et bouton, exercice réparé en tête de la validation avec son badge, formule rendue.

### 2. `isFormula` élargi
- Nouveaux critères, dans l'ordre : `$` ou `\` ; `=`, `^` ou `_` ; lettre grecque Unicode ; chiffre collé à une lettre (`2x`, `10m`) ; quotient de lettres (`m/s`, `J/K`) ; exposants et fractions Unicode (`²`, `½`) ; puis l'ancien critère « symboles ≥ lettres ». Résultat : `F = ma (deuxième loi de Newton)`, `E_c = ½mv² (énergie cinétique)`, `div E = rho/epsilon0` → formule ; « Le champ est nul à l'intérieur d'un conducteur à l'équilibre » → texte.
- `typedMatch(input, answers, { forceFormula })` : le joueur de flashcard force la comparaison « formule » quand l'exercice porte un point de nature `formule` ou `theoreme` (lu en base par `pointId`) ou un tag `formule` ; une réponse en prose sur un tel point ne reçoit alors jamais de « Bien » par similarité. Tests `typed.test.ts` (les quatre exemples, lettre grecque, `2x`, `m/s`, `F = -ma` sans suggestion, `forceFormula`).

### 3. Dispersion des intervalles hors examen
- Test unitaire (`fsrs.test.ts`) : carte S = 5, état Review, révisée à échéance, rétention 0,90, sans plafond → **Difficile 12 j / Bien 17 j / Facile 28 j** (Encore 10 min), soit 0,71 × Bien et 1,65 × Bien : le planificateur n'applique aucun plafond parasite. Test équivalent via `loadSessionContext` + `schedulerFor` sans examen (`session.test.ts`) : même dispersion, `schedulerFor` renvoie le planificateur général, pas de plafond signalé.
- Le resserrement observé lors de la vérification finale de la Revue 1 (9 / 10 / 11 j) venait bien de l'examen « DS n° 2 » à J−21 du cahier de démonstration : plafond ⌊21/2⌋ = 10 j, puis écartement forcé ±1 j (même carte, plafond 10 : 10 / 11 / 12 j ; 9 / 10 / 11 j avec le décalage des jours légers).
- Interface : `intervalCap(ctx, exercice, carte)` compare l'aperçu plafonné à l'aperçu du planificateur général et ne signale que les boutons réellement contraints ; `overridesFor` retient le nom de l'examen le plus strict. Les joueurs flashcard et démonstration affichent « ⌃ examen » sous l'intervalle concerné, avec au survol « Intervalle plafonné à N j par l'examen X ». Test : examen à J+21 sur une carte S = 30 → `days` 10, `examName`, `good` et `easy` marqués, `again` non. Vérifié dans le navigateur : « Difficile 8 j ⌃ examen / Bien 9 j ⌃ examen / Facile 10 j ⌃ examen », info-bulle « Intervalle plafonné à 10 j par l’examen DS n° 2 », « Encore 10 min » sans marqueur.

### 4. Sauvegardes de migration : bases déjà en v5
- Réglages → « Sauvegardes de migration » n'est plus masqué quand la liste est vide : la section affiche « Aucune sauvegarde de migration : cette base a été migrée avant l'ajout de cette protection » avec un bouton « Exporter maintenant » (même export JSON complet que « Exporter une sauvegarde »). Rien d'autre ; pas de test unitaire (rendu React seul), vérifié dans le navigateur sur la base de démonstration (0 sauvegarde).

### 5. Fixture linter : lot non écrit par moi
- `tests/fixtures/lot-externe/README.md` : comment déposer une réponse brute de Claude (`.txt`, un fichier par lot), lancer le test, lire le rapport, compter soi-même faux positifs et défauts manqués. `.txt` et `rapport.md` ignorés par git (les fiches restent locales).
- `lint.external.test.ts` : ignoré proprement (`it.skip`, 1 test « skipped ») quand le dossier ne contient aucun `.txt` ; sinon parse chaque fichier comme l'import (`parseClaudeResponse`, réparations comptées), applique `lintBatch` et écrit `rapport.md` : points, exercices, rejetés au parsing (raison + texte brut), antislashs réparés, occurrences par code (libellé, sévérité), liste des exercices signalés avec leur texte (énoncé → réponse), puis la liste des exercices non signalés pour repérer les défauts manqués. Aucun seuil.
- Vérifié : dossier vide → 1 skipped ; fixture physique copiée temporairement en `.txt` → rapport de 81 lignes (40 exercices, 0 rejeté, 20 signalés, 22 occurrences sur 11 codes), puis fichier et rapport retirés.

### 6. Rapport final (exécuté le 2026-09-12, après le commit de la section 5)
1. **Tests et build** : fait — 24 fichiers, 175 tests verts + 1 ignoré volontairement (`lint.external.test.ts`, dossier vide) ; `npm run build` vert (PWA, 43 entrées pré-cachées). 146 tests au départ du lot, 175 à la fin.
2. **`repairJson`** : fait — les 13 commandes de la section 1, chacune avec un antislash simple dans `$…$`, après réparation puis `JSON.parse` :

| Commande (antislash simple, dans `$…$`) | Après réparation + `JSON.parse` | Antislashs doublés | Sans réparation (`JSON.parse` direct, avant ce lot) |
|---|---|---|---|
| `\frac{1}{2}` | `$\frac{1}{2}$` ✓ | 1 | `$⟨FF⟩rac{1}{2}$` |
| `\forall x` | `$\forall x$` ✓ | 1 | `$⟨FF⟩orall x$` |
| `\nabla f` | `$\nabla f$` ✓ | 1 | `$⟨LF⟩abla f$` |
| `a \neq b` | `$a \neq b$` ✓ | 1 | `$a ⟨LF⟩eq b$` |
| `\theta` | `$\theta$` ✓ | 1 | `$⟨TAB⟩heta$` |
| `\tau` | `$\tau$` ✓ | 1 | `$⟨TAB⟩au$` |
| `2 \times 3` | `$2 \times 3$` ✓ | 1 | `$2 ⟨TAB⟩imes 3$` |
| `\text{si}` | `$\text{si}$` ✓ | 1 | `$⟨TAB⟩ext{si}$` |
| `\rho` | `$\rho$` ✓ | 1 | `$⟨CR⟩ho$` |
| `\left( x \right)` | `$\left( x \right)$` ✓ | 2 | JSON invalide (`\l`), puis l'ancienne réparation gardait `\r` : `\left( x ⟨CR⟩ight)` |
| `\beta` | `$\beta$` ✓ | 1 | `$⟨BS⟩eta$` |
| `\bar{x}` | `$\bar{x}$` ✓ | 1 | `$⟨BS⟩ar{x}$` |
| `\begin{cases} a \end{cases}` | `$\begin{cases} a \end{cases}$` ✓ | 2 | JSON invalide (`\e`), puis `⟨BS⟩egin{cases} a \end{cases}` |

   Hors contexte : `"a\nb"` reste un saut de ligne, `"\theta"` devient `\theta` ; non-régression octet à octet sur un JSON correct (`\\frac`, `\"`, `é`, `\u00e9`, `\n` réel) : texte identique, `{ doubledBackslashes: 0, trailingCommas: 0 }`.
3. **Import** : fait — bandeau « 4 antislashs réparés — vérifie les formules (\eta, \frac, \theta, \ce) » vu dans le navigateur ; « Copier les rejetés » produit « Cet élément (exercices) de ta réponse a été rejeté par l'application pour les raisons indiquées. Renvoie-le corrigé, au même format JSON … » suivi de « 3. Raison : Texte à trous sans {{trou}} » et du bloc ```json de l'élément ; l'exercice réparé arrive en tête de la validation avec le badge.
4. **`isFormula`** : fait — `F = ma (deuxième loi de Newton)` → formule ; `E_c = ½mv² (énergie cinétique)` → formule ; `div E = rho/epsilon0` → formule ; « Le champ est nul à l'intérieur d'un conducteur à l'équilibre » → texte (tests `typed.test.ts`).
5. **Dispersion hors examen** : fait — S = 5, Review, à échéance, rétention 0,90 : Difficile 12 j / Bien 17 j / Facile 28 j (Encore 10 min), soit 0,71 × et 1,65 × Bien ; aucun plafond parasite dans le planificateur ni dans `schedulerFor`. Marqueur « ⌃ examen » affiché et info-bulle « Intervalle plafonné à 10 j par l’examen DS n° 2 » vus dans le navigateur sur les trois boutons contraints.
6. **Réglages** : fait — « Aucune sauvegarde de migration : cette base a été migrée avant l'ajout de cette protection » + « Exporter maintenant » vus sur la base de démonstration (0 sauvegarde).
7. **`lint.external.test.ts`** : fait — dossier vide → 1 test ignoré (« skipped »), suite verte ; fixture physique copiée temporairement en `.txt` → `rapport.md` généré (81 lignes : 40 exercices, 0 rejeté, 20 signalés, 22 occurrences sur 11 codes), fichier et rapport retirés ensuite.

Correction d'un chiffre de la Revue 1 (section 4) : la fixture physique annote 22 défauts sur 20 exercices, pas « 19 sur 17 » ; précision et rappel de 1,00 inchangés.
Non fait / partiellement : rien dans ce lot.

## Nuit 1 — Design en profondeur + accès mobile (session autonome)

Règle de décision appliquée aux choix non tranchés : données préservées > local sans serveur > simple > performant.

### A0. Outillage et tokens
- Thème marine par défaut (`DEFAULT_SETTINGS.theme = 'dark'`) : c'est la signature ; les utilisateurs existants gardent leur réglage stocké.
- Les utilitaires Tailwind historiques (`bg-surface`, `text-ink`, `text-muted`, `border-line`, `bg-accent-soft`…) sont conservés comme alias des nouveaux tokens : chaque page migre à son étape sans casser les autres.
- Un seul accent (ambre) ; `ok` = ambre, `bad` = corail désaturé, `warn` = ambre (la palette n'a pas de troisième couleur, le pictogramme fait la différence).
- Sur thème clair, l'ambre en texte devient `--accent-text` (#8f5f0c) pour tenir le contraste AA ; l'ambre plein garde un texte sombre.
- Dégradé, vignette et grain sont en CSS (`body::before`, `body::after` fixes) et non sur le canvas du fond : le canvas ne dessine que les particules.
- Crochet `window.__cahiers` toujours présent (import de sauvegarde, thème) : minuscule, et ce sont des actions déjà offertes par Réglages ; un flag de build aurait compliqué les scripts.
- Page `/design` chargée paresseusement et seulement en développement (`import.meta.env.DEV`) : absente du build ; les captures de cette page passent par le serveur de dev (`screenshots.mjs --dev`).
- Lighthouse : `chrome-launcher` échoue à lancer le Chromium de Playwright sur cette machine (`spawn UNKNOWN`) ; le script lance Chromium via Playwright avec `--remote-debugging-port` et Lighthouse s'y connecte.
- Base de démonstration à 65 exercices (au lieu de ~80) : contenu réel de prépa uniquement, pas de remplissage.

### A1. Champ d'équations
- Rendu : un seul canvas, sprites pré-rasterisés (une fois par texte × plan × thème), `drawImage` seul dans la boucle, une rasterisation au plus par frame, cache LRU 120, vocabulaire actif de 24 formules ré-échantillonné toutes les deux minutes (sinon le cache tourne en boucle sur 200 textes × 5 plans).
- Dégradation automatique : médiane de 60 frames au-dessus du budget (2 ms desktop, 3 ms mobile) → plan le plus lointain retiré → 60 % des particules → statique ; remontée d'un cran après 10 s sous 60 % du budget.
- Mesure de performance avec compositing GPU (`--enable-gpu`) : en headless SwiftShader, tout canvas plein écran plombe le fps quel que soit le moteur ; ce n'est pas le comportement d'un téléphone. Les deux chiffres sont consignés.
- Convertisseur LaTeX → Unicode conservateur : commande inconnue, indice/exposant de plus de 4 caractères, fraction dont un membre dépasse 8 caractères ou résultat > 28 caractères → formule écartée. Exposant sans équivalent Unicode → forme `x^(…)` (l'exemple du brief `e^(iπ)`), indice sans équivalent → écrit à plat (`E_c` → `Ec`), `^\circ` → `°`.
- Polices en `font-display: optional` déclarées à la main (pas via les CSS fontsource, qui imposent `swap`) et premier rendu après chargement des polices (plafond 800 ms) : CLS 0 au swap ; sur une toute première visite sans cache, le navigateur peut garder la police de repli jusqu'au rechargement.
- Gyroscope : jamais de demande automatique ; la case « Activer le mouvement » de Réglages déclenche `DeviceOrientationEvent.requestPermission()` dans le clic (iOS).

### A2. Composants
- `ui.tsx` garde exactement les mêmes exports et props (plus des ajouts) : les pages migrent visuellement à leur étape A3 sans réécriture préalable.
- Un seul jeu d'icônes (lucide-react), migration par script avec table de correspondance ; `weight` de Phosphor supprimé (lucide n'a qu'une graisse de trait).
- Thème clair : un seul ambre `#8f5f0c` pour le remplissage ET le texte (AA des deux côtés), texte blanc sur bouton primaire ; l'ambre lumineux `#f2b75c` reste réservé au thème marine.
- Toasts : store module (pas de contexte React) pour pouvoir appeler `toast()` depuis les libs ; 4 toasts max, 4 s (6 s en erreur).
- Tooltip en CSS pur (survol + focus-within, `aria-describedby`) : pas de dépendance, pas de portail.
- Test `session.test.ts` (hypercorrection) rendu indépendant de l'heure : il supposait que l'étape de réapprentissage de 10 min tombe avant le minuit suivant, faux entre 23 h 50 et minuit (échec constaté à 23 h 53). `now` fixé à 10 h. Aucune logique métier touchée.

### A3.1–A3.2. Tableau de bord et session
- « Connaissance conservée » sur le tableau de bord = rétrievabilité moyenne FSRS des cartes actives (même calcul que la page Statistiques), avec les plafonds d'examen ; calculé dans un `useMemo`, ≈ 1 ms pour 65 cartes.
- Les examens à venir passent sous les cahiers : c'est le seul bloc dont l'existence n'est connue qu'après chargement ; placé en dernier, son apparition ne décale rien (CLS).
- `GradeButtons` : un seul composant pour les trois joueurs auto-notés ; les raccourcis clavier restent dans chaque joueur (ils dépendent de l'état local : révélé, correction ouverte…).
- Inclinaison 3D : `TiltCard` n'anime que `transform` et un dégradé radial en `opacity` ; désactivée sur pointeur grossier et en reduced-motion ; jamais sur les cartes de contenu des pages (coût GPU mobile), seulement la carte de session.
- View Transitions activées sur les liens de navigation via la prop `viewTransition` de React Router (pas de wrapper maison) ; CSS global de 200 ms.

### B1. Tactile
- Barre inférieure mobile à quatre entrées ; « Cahiers » a sa page (`/cahiers`) car le tableau de bord la met trop bas sur un téléphone.
- Cibles tactiles : règle CSS globale sous `(pointer: coarse)` (`min-height/min-width: 44px` sur les boutons) plutôt qu'une retouche composant par composant ; les `kbd` sont masqués (pas de clavier).
- Balayage des flashcards : geste maison (Pointer Events, `pointerType === 'touch'`) plutôt que `drag` de motion, pour rester en `transform` pur et ne rien changer aux clics ; seuil 40 % de la largeur de la carte ; réglage « Balayer les flashcards » (activé par défaut, propre à l'appareil).
- Session sur téléphone : la carte est alignée en bas de l'écran (`justify-end`), les boutons de note tombent sous le pouce, sans barre fixe séparée qui aurait cassé la structure des dix joueurs.
- Aide « ? » : page `/aide` sur mobile (bouton « Aide » sous l'exercice), modale conservée au clavier.
- Partage : `navigator.share({ text })` seulement sur pointeur grossier ; repli automatique sur copie + lien si la feuille est annulée ou refusée.

### B2. Accès mobile (ADR)
- Décision consignée dans `docs/adr/0001-acces-mobile.md` : hébergement statique (GitHub Pages) **et** synchronisation OneDrive « dossier d'application » avec fusion locale ; mode dégradé sans compte : export/import fusionnant. Non retenus : CouchDB (serveur à maintenir), pair-à-pair (les deux appareils allumés en même temps), Supabase/Firebase (cours chez un tiers).

### B3. Moteur de fusion
- Tombstones dans une table séparée (`tombstones`, clé `[table+id]`) au lieu d'un `deletedAt` sur chaque ligne : les suppressions restent de vraies suppressions pour tout le code existant, rien à filtrer. Écart assumé par rapport à l'énoncé (« `deletedAt` conservé 90 jours ») : même durée, même sémantique, autre emplacement.
- Suppression d'un cahier / d'une fiche : un tombstone par ligne enfant, pas de cascade dans le moteur de fusion, pour garder l'associativité (une cascade à la fusion dépendrait de l'ordre des fusions quand un parent ressuscite).
- Exercices révisés des deux côtés : rejeu du journal réuni avec ts-fsrs (déterministe, fuzz coupé), plutôt que « le plus récent gagne » qui perdrait les réponses de l'autre appareil. Le rejeu repart d'une carte vierge : sur une carte migrée de SM-2, l'état peut s'écarter un peu de ce que l'un des appareils affichait.
- `deviceId` posé par un middleware Dexie (DBCore) plutôt qu'à chaque écriture du code : impossible d'en oublier une ; désactivé par un marqueur `noStamp` sur la transaction d'import ou de fusion. L'identifiant vit dans `localStorage` (le middleware est synchrone).
- Réglages : fusion par clé avec tampons dans `kv` ; à la migration v6, toutes les clés sont datées du moment de la migration (une sauvegarde plus ancienne ne les écrase pas).
- `SCHEMA_VERSION` des sauvegardes : 4 → 6, aligné sur la version Dexie (la sauvegarde `backup_before_v6` porte 5 et doit être acceptée).
- Un exercice supprimé d'un côté emporte son journal dans la fusion, comme une suppression locale : les statistiques restent sans lignes orphelines.

### B4. Fournisseurs de synchronisation
- Un seul fichier contesté (`manifest.json`, `If-Match`) ; instantanés et lots sont créés, jamais réécrits (`conflictBehavior=fail`). Un conflit efface les fichiers de la ronde et recommence (3 essais) : pas de verrou, pas d'état à moitié appliqué (chaque fusion est une transaction Dexie).
- Sous-dossier `sync` dans le dossier d'application plutôt que sa racine, pour laisser la place à d'autres usages (sauvegardes complètes datées, par exemple) sans polluer l'index.
- Lecture OneDrive par `@microsoft.graph.downloadUrl` (URL pré-authentifiée, sans jeton) plutôt que `:/content` : la réponse `:/content` est une redirection 302 dont `fetch` perd l'ETag.
- Jeton et `fetch` injectés dans le fournisseur : testable sans MSAL ni réseau ; l'app passe `acquireToken(clientId)` de `graph.ts` (silencieux puis popup), donc la première ronde après l'ajout de la portée `Files.ReadWrite.AppFolder` redemandera un consentement une fois.
- Le journal `reviewLogs` est tamponné `deviceId` comme les autres tables : un appareil ne pousse que ses propres réponses (sans cela, les réponses reçues seraient renvoyées à chaque ronde).
- Instantané précédent gardé une génération : un appareil qui tient encore l'ancien manifest peut le lire ; s'il manque quand même (deux générations de retard), la ronde recommence avec le manifest à jour.
- Réglages de synchronisation (fournisseur, automatique) dans `kv`, pas dans `settings` : propres à l'appareil et sans re-rendu des pages qui lisent les réglages.

### B5. Hébergement statique
- Un seul réglage, `VITE_BASE`, lu au build : GitHub Pages sert sous `/<dépôt>/`, Cloudflare ou le développement local sous `/`. Tout ce qui dépend du chemin (manifest, service worker, routeur, MSAL) le lit depuis `base` / `import.meta.env.BASE_URL` : aucune adresse absolue codée en dur.
- Routage : `404.html` copie d'`index.html` (adresses propres, zéro redirection) plutôt que le mode hachage. Le statut 404 renvoyé par GitHub Pages sur un lien profond est sans effet pour une app personnelle (pas de référencement à préserver).
- URI de redirection MSAL = origine **+ base** (`https://moi.github.io/cahiers/`), pas seulement l'origine : sur GitHub Pages l'origine seule serait la racine du compte (autre site ou 404). Une inscription Entra accepte plusieurs URI : locale et hébergée coexistent.
- Le workflow lance les tests avant le build : un déploiement ne part pas sur une suite rouge.

### B6. PWA mobile
- Icônes et écrans de lancement générés par le script maison plutôt qu'un paquet (`pwa-asset-generator` tire Puppeteer) : neuf tailles iOS suffisent (iPhone 8 → 15 Pro Max, deux iPad) ; les autres appareils prennent le fond `background_color`.
- Couleurs de l'icône alignées sur le design (marine + ambre) : le bleu `#3b5bdb` datait d'avant A0 et jurait sur l'écran d'accueil à côté du thème. Doute de goût consigné dans le rapport.
- Deux `meta theme-color` à requête média plutôt qu'une seule réécrite par script : le thème automatique suit le système sans JavaScript, et `applyTheme()` ne touche aux deux que quand le thème est forcé.
- Bannière d'installation dans le flux du tableau de bord (pas une modale, pas un toast) : proposée une fois, fermable, jamais en mode standalone ; iOS reçoit des instructions puisque Safari n'a pas d'événement d'installation.
- `navigator.storage.persist()` redemandé au lancement seulement s'il y a des données et que la persistance n'est pas acquise : sans données il n'y a rien à protéger, et Chrome n'accorde de toute façon la persistance qu'aux sites installés ou souvent visités.
